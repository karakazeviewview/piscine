#include <stdio.h>
#include <string.h>

size_t strLcpy(char	*dst, const	char	*src, size_t size)
{
	char	*pd;
	const	char	*ps;
	char	*pde=dest+seze-1;

	for(pd=dst, ps=src;*ps!='\0'&&pd<pde;pd++,ps++)
	
	{
		*pd='\0';
	}

	while(*ps++);

	while(*ps++);
	retur(ps-src-1);
}

size_t strLcat(char	*)
